<div class="col-12 col-md-4 mb-4">
  <a href="/video/<%= video.shortUUID %>" class="video-card card bg-dark shadow">
    <img src="https://<%= domain %><%= video.previewPath %>" class="card-img-top" alt="<%= video.name %>" rel="nofollow">
    <div class="card-body">
      <h5 class="card-title text-success fs-5 fw-semibold"><%= video.name %></h5>
      <p class="card-text"><%= video.description %></p>
    </div>
  </a>
</div>