/**
 * Enviroment
 */
export enum Env {
  PRODUCTION = 'production',
}

/**
 * Authorization mode
 */
export enum AuthMode {
  STRICT = 'strict',
  CREDENTIALS = 'credentials',
  PEERTUBE = 'peertube',
  MIXED = 'mixed',
}
